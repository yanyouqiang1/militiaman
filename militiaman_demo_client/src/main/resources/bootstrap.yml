server:
  port: 9988
spring:
  application:
    name: demo-client

  cloud:
    config:
        uri: http://localhost:8001
        name: demoClient

    consul:
      host: localhost
      port: 8500
      discovery:
        register: false

